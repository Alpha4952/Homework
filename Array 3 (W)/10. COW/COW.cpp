#include <bits/stdc++.h>
using namespace std;

long long n, d, a[100001], aa, bb;
pair <long long, long long> h;

int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	freopen("COW.inp", "r", stdin);
	freopen("COW.out", "w", stdout);

	cin >> n >> d;
	for (int i = 0; i < n; i++) {
		cin >> aa >> bb;
	}
	for (int i = 0; i < n; i++) cout << a[i];
}